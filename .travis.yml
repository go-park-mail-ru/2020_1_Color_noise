language: go

go: "1.13"

env: GO111MODULE=on

notifications:
  email: false

before_script:
  - docker build -t db --file `pwd`/build/db.Dockerfile .
  - docker run -d db
  - go mod tidy

script:
  - go test ./...